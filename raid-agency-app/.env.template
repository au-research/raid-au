PORT=7080

BASE_URL=http://localhost:7080

# Enable ARDC branding override from public/app-config.json
VITE_APP_USE_CUSTOM_CONFIG=true

# E2E test credentials (not runtime config — used by Playwright only)
VITE_KEYCLOAK_E2E_USER=raid-test-user
VITE_KEYCLOAK_E2E_PASSWORD=

# Service URLs, Keycloak config, and environment settings are now in
# public/runtime-config.json — edit that file for local dev or mount it
# per-deployment in production.
