# Secrets — never put these in app-config.json or commit them.
# Copy this file to .env and fill in your values.
#
# All non-secret configuration (API endpoints, environment name, branding,
# analytics IDs, etc.) lives in public/app-config.json.
# See app-config.template.json for a starter template.

IAM_CLIENT_SECRET=
RAID_DUMPER_CLIENT_SECRET=
